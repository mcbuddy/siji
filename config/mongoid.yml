

development:
  sessions:
    default:
      database: siji
      hosts:
      - localhost:27017

production:
  sessions:
    default:
      url: <&= ENV['MONGOLAB_URI] &>
      options:
        consistency: :strong
        max_retries: 1
        retry_interval: 0